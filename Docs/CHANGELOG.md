# Ollamantis Change Log 📋

## v1.0.1 *(current)* 🆕
 - `OllamaClient` class: renamed property `EndpointBase` to `Host`.
 - Fixed exception when calling static method `EnvironmentVariables.ToString()` due wrong `KeepAliveOption` type casting.

## v1.0
Initial Release.